'use client'

interface PopularProductsProps {
  products: unknown[]
}

export default function PopularProducts(_props: PopularProductsProps): React.JSX.Element {
  // Hidden - not in reference design
  return <div className="hidden" />
}
