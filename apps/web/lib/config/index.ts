/**
 * Configuration Barrel Export
 *
 * @description Central export point for all configuration modules.
 * Currently exports API configuration (base URLs, endpoints, timeouts).
 *
 * @module lib/config
 *
 * @example
 * import { API_CONFIG, API_ENDPOINTS } from '@/lib/config'
 */

export * from './api'
